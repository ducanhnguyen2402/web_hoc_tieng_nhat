# Quy tắc dự án (Project Rules)

1. **Ngôn ngữ:** Sử dụng TypeScript chặt chẽ (strict mode).
2. **Kiến trúc Component:** Chia nhỏ component, giữ component gọn gàng và tái sử dụng được.
3. **Naming Convention:** Đặt tên biến, hàm, file, folder bằng tiếng Anh.
4. **Giao diện (UI):** Các đoạn text hiển thị cho người dùng phải dùng tiếng Việt.
5. **Font chữ:** Phải hỗ trợ hiển thị tốt tiếng Nhật (dùng font Noto Sans JP).
6. **Kiểm tra nghiệm thu:** Mỗi thay đổi lớn hoặc tính năng mới phải được chạy thử thành công (không có lỗi build/runtime) trước khi kết thúc task.
